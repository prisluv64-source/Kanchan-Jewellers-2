name: Add enquiry form

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/add-enquiry-form.yml'
      - 'scripts/add_enquiry_form.py'

permissions:
  contents: write

jobs:
  add-enquiry-form:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Add enquiry form
        run: python scripts/add_enquiry_form.py

      - name: Commit enquiry form changes
        run: |
          if git diff --quiet -- contact.html; then
            echo "No enquiry form change needed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add contact.html
          git commit -m "Add WhatsApp-backed enquiry form"
          git pull --rebase origin main
          git push
